# Código original gerado pela IA — preservado

Snapshot intacto da primeira geração, salvo em 2026-09-04 antes da revisão.
Mantido apenas como referência/comparação. **Não execute os arquivos daqui** —
contêm os defeitos documentados na revisão (exclusão irreversível de e-mail em
`neon_interceptor.py` e execução remota de comandos sem autenticação em
`src/routes/security.routes.ts`).

Comparar com a versão corrigida:

    diff -u original_ia/neon_interceptor.py neon_interceptor.py
